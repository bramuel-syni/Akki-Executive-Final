"""Backend models package — Pydantic schemas that don't fit cleanly
inside a single router or service module live here.

Track A Phase 1 (2026-06-03) — first occupant: the new `Analysis`
entity (`models.analysis.Analysis`), which is the multi-file
successor to the P5.14 `WorkbookAnalysis`. See `models/analysis.py`.
"""
